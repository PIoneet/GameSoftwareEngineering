# CLAUDE.md — 잠든 것의 이름 (오픈월드 쿼터뷰 RPG)

> 이 파일은 `BaseProject_SE` 프로젝트 전용 영구 컨텍스트입니다.
> 작업 시작 시 항상 이 문서를 먼저 읽고, 여기 기준으로 판단합니다.
> 새 결정/구조 변경이 있으면 이 문서를 갱신합니다.
>
> ⚠️ **상위 폴더 `C:\Users\sunho\HongLab\CLAUDE.md`는 별개의 프로젝트(유니티 기말과제, "Ellen" 3D 액션게임)
> 문서입니다. 이 프로젝트(`BaseProject_SE`)와는 무관하니 적용하지 않습니다.**

---

## 1. 프로젝트 개요

- **출발점:** 수업(Game Software Engineering, KPU)에서 배포한 C++/OpenGL 베이스 프로젝트("SimpleGame")
- **현재 코드베이스:** GLEW + FreeGLUT 기반 최소 렌더러 스켈레톤. `Renderer` 클래스가 셰이더 컴파일/VBO 생성/사각형 그리기를 담당 ([Renderer.h](SimpleGame/Renderer.h), [Renderer.cpp](SimpleGame/Renderer.cpp)), `main()`은 GLUT 콜백만 등록된 상태 ([SimpleGame.cpp](SimpleGame/SimpleGame.cpp)).
- **저장소:** `https://github.com/PIoneet/GameSoftwareEngineering.git` (master 브랜치)
- **목표:** 이 스켈레톤을 확장해 **실시간 렌더링 기반의 오픈월드 RPG**를 제작한다.

---

## 2. 게임 디자인 목표

| 항목 | 결정 사항 |
|---|---|
| 장르 | 오픈월드 RPG |
| 시점 | 쿼터뷰(quarter-view), **2.5D**로 표현 |
| 구현 방식 | **2D 스프라이트 + 아이소메트릭 수식** (진짜 3D 카메라/MVP 행렬 아님) — world(x,y,z)를 CPU에서 화면 좌표로 변환, 깊이 정렬은 `GL_DEPTH_TEST`가 아니라 **painter's algorithm(그리기 순서 정렬)**으로 처리 |
| 플레이 인원 | **싱글 플레이 전용** — 네트워킹/서버 동기화 불필요 |
| 렌더링 | 실시간(real-time) — 프레임마다 Update(dt) → Render() 루프 필요 (현재 코드엔 아직 없음, 추가 예정) |

---

## 3. 세계관 / 스토리 바이블

- **시간적 배경:** 중세 판타지
- **분위기(톤):** 다크하지만 군데군데 유머가 섞임 (예: 길드 서기의 관료주의 개그, 동료 NPC의 드립)
- **스토리 축:** 미지의 존재를 조금씩 알아가는 코즈믹 호러 — **크툴루 신화 오마주**. "알수록 무거워지는 진실" 모티프.
- **전투/던전 설계 철학:** **던전밥(던전 밥) 스타일** — 몬스터를 단순 스탯 덩어리가 아니라 각자 고유한 생태·행동 패턴·**공략법(counter-strategy)**을 가진 대상으로 설계. 데이터 기반 베스티어리(종류별 스탯+행동+공략법) 구조를 지향.
- **작업 타이틀:** "잠든 것의 이름" (가제 — 변경 가능)

### 메인 퀘스트 트리 (초안 v0.1)

- **서장 — 이방인의 밤:** 축제 밤, 낯선 손님 등장 + 슬라임 소탕 첫 의뢰. 손님이 남긴 소용돌이 문양 동전(첫 단서).
- **1막 — 이빨 자국:** 실종 사건 조사, 첫 던전(채석장). 초커/아이볼 등장. 문양 재발견.
- **2막 — 잠들지 않는 자들의 노래:** 오픈월드 자유 탐사(3개 지역, 순서 무관). 사이비 신도/촉수 유생체 등장. 원로의 배신 밝혀짐.
- **3막 — 이름 없는 것의 그림자:** 최종 던전(가라앉은 사원), 3페이즈 보스 "심연의 파수꾼". 존재의 정체가 "파괴자가 아니라 잊히길 원하는 무언가"임이 드러남.
- **파이널 — 잠든 것의 이름을 부르다:** 3분기 엔딩 — **봉인(A)** / **이해(B)** / **파괴(C)**.

> 상세 내용은 발행된 아이디어 아트팩트("잠든 것의 이름 — 메인 퀘스트 트리") 참고. 지역/던전 개수, 분기 조건 등 세부는 추후 리비전에서 구체화 예정.

---

## 4. 구현된 구조 (튜토리얼 프로토타입, 2026-09)

### 4-1. 확정된 기술 규약

- **깊이 = `x + y` 만으로 결정한다.** 높이(`z`)는 화면 Y를 위로 밀 뿐 정렬 키에 넣지 않는다.
  넣으면 언덕 위/아래 오브젝트의 앞뒤 관계가 뒤집힌다. → 이것이 "2.5D"의 실체.
- **입력은 상태 버퍼를 거친다.** GLUT 콜백은 이벤트 단발이라 동시 입력·홀드를 표현하지 못한다.
  콜백은 `Input` 배열만 갱신하고, 게임 로직이 매 프레임 폴링한다.
- **지면 타일은 정렬하지 않는다.** 평면 마름모는 서로 겹치지 않으므로 아무 순서로 그려도 된다.
  정렬 대상은 높이가 있는 것(오브젝트·캐릭터)뿐이다.
- **깊이 테스트는 끈다** (`glDisable(GL_DEPTH_TEST)`). 스프라이트는 알파가 있어 Z-buffer와 상성이 나쁘다.

### 4-2. 렌더 파이프라인

```
BeginScene()   → 씬을 오프스크린 FBO 에 그린다
EndScene()
PostProcess()  → 밝은 영역 추출 → 가로/세로 블러(절반 해상도) → 합성
                 (블룸 + 색보정 + 비네트 + 필름 그레인, dread 에 반응)
UI             → 보정 위에 직접 그린다
```

### 4-3. 파일 구성

| 파일 | 역할 |
|---|---|
| `IsoMath.h` | 아이소메트릭 좌표 변환 (`TILE_W` 64, `TILE_H` 32) |
| `Input.h/.cpp` | 키 상태 버퍼. `Held()` / `Pressed()` |
| `Font.h/.cpp` | GDI 로 한글 글리프를 텍스처 아틀라스에 런타임 캐싱 |
| `World.h/.cpp` | 112×112 절차적 맵 — 호수·숲·길, 마을/밭 배치, 공간 격자 충돌 |
| `Draw.h/.cpp` | 그리기 전담 — 지형 디테일, 건물 머터리얼, 캐릭터·짐승 파츠 애니메이션 |
| `Game.h/.cpp` | 플레이어·NPC·짐승·퀘스트·대화·UI (로직) |
| `Renderer.h/.cpp` | FBO 사후처리, 스프라이트/마름모, 텍스처 쿼드, 한글 텍스트 |
| `Shaders/Sprite.*` | 픽셀 좌표 → NDC + 단색 |
| `Shaders/Textured.*` | 글리프 등 텍스처 쿼드 |
| `Shaders/Post.vs` | 사후처리 공용 풀스크린 정점 |
| `Shaders/Bright.fs` | 블룸용 밝은 영역 추출 |
| `Shaders/Blur.fs` | 분리형 가우시안 블러 |
| `Shaders/Composite.fs` | 블룸 합성 + 색보정 + 비네트 + 그레인 |

### 4-4. 주의할 함정

- **`DrawText` 는 Win32 매크로다** (`winuser.h` 가 `DrawTextW` 로 치환).
  멤버 함수 이름으로 쓰면 windows.h 를 포함한 번역 단위에서만 이름이 달라져
  "멤버가 아닙니다" 로 터진다. → `DrawString` 으로 명명. `GetObject`, `LoadImage` 등도 동일.
- **Windows 타입은 헤더에 노출하지 않는다.** `Font.h` 는 `HDC`/`HFONT` 를 `void*` 로 감춰
  windows.h 가 다른 번역 단위로 새지 않게 한다.
- **글리프는 그리기 전에 미리 구워야 한다.** 드로우 루프 안에서 `Bake` 가 돌면
  텍스처 바인딩이 바뀌어 뒤따르는 글자가 깨진다.

### 4-5. 남은 과제

- **오픈월드 확장:** 지역 이동, 월드 스트리밍은 아직 없음.
- **전투/인벤토리:** 몬스터 베스티어리와 전투 루프 미구현 (짐승은 배회·도주 AI 만).
- **오디오:** 없음.

---

## 5. 협업 규칙

- **빌드/실행 확인은 사용자가 직접 진행.** 특별한 이유(사용자 요청, 빌드 에러 디버깅 등)가 없으면 Claude는 코드 수정까지만 하고 빌드/실행을 시도하지 않는다.
- 요구사항은 한 번에 몰아서 주지 않고 순차적으로 추가되는 경우가 많음 — 사용자가 "더 말씀드릴게요"라고 하면 구현에 들어가지 않고 스펙을 계속 수집한다.

### 5-1. Git 사용 규칙 (2026-09-14 추가)

커밋은 사용자가 직접 관리한다. Claude 는 요청 없이 커밋하지 않는다. 다만 아래는 반드시 지킨다.

- **대규모 덮어쓰기 전 `git status` 확인.** 커밋되지 않은 변경이 있으면 덮어쓰기 전에 사용자에게 알리고
  커밋을 권한다.
  > 실제 사고 사례: 2026-09 업그레이드 작업 때 1차 프로토타입이 커밋되지 않은 상태에서
  > `Renderer.cpp` / `Game.cpp` / `World.cpp` 를 통째로 덮어썼다. 작동하던 코드의 유일한 사본이
  > 사라질 뻔했다. 사용자가 알아서 커밋해둔 덕에 넘어간 것이지 안전했던 게 아니다.
- **넘겨드리기 전 `git diff` 리뷰 패스.** 자기가 방금 쓴 파일을 다시 읽는 것과, 변경분을
  *변경으로서* 보는 것은 다른 검사다. 대량 수정·일괄 개명 후에는 grep 이 아니라 diff 로 확인한다.
- **시각적 마일스톤마다 커밋 제안.** 렌더링 작업은 스크린샷 기반 반복이라
  "조명 조정 전/후" 같은 A/B 비교가 가능해야 한다.
- **커밋 단위를 좁게.** 2026-09 업그레이드는 28파일 4,316줄이 한 커밋에 들어갔다.
  이러면 문제가 생겨도 원인을 좁힐 수 없다. 기능 단위로 끊는다.

---

## 6. 코드 컨벤션

### 6-1. 빈 줄 (가장 중요)

빽빽한 코드는 읽히지 않는다. 특히 드로잉 코드는 호출이 줄줄이 이어져 벽이 되기 쉽다.

- **주석 앞에는 빈 줄.** 단, 블록(`{`)의 첫 줄이면 붙여 쓴다.
- **여러 줄에 걸친 호출 뒤에는 빈 줄.** 어디서 한 호출이 끝났는지 눈에 보여야 한다.
- **`break;` 앞에 빈 줄.** case 블록의 끝을 분명히 한다.
- **선언 묶음과 실행 코드 사이에 빈 줄.**
- **논리적 단계마다 빈 줄.** 드로잉은 `그림자 → 본체 → 디테일 → 하이라이트` 순서가
  빈 줄로 드러나야 한다.
- **`return` 앞에 빈 줄.** 단, 본문이 한 줄뿐인 함수는 예외.

**예외 — 나란한 한 줄짜리는 표처럼 붙인다.** 같은 모양의 구문이 줄줄이 늘어설 때는
빈 줄로 끊는 쪽이 오히려 읽기 나쁘다. 세로로 정렬해 표로 만든다.

```cpp
case TILE_PATH:    cr = 0.246f; cg = 0.209f; cb = 0.156f; break;
case TILE_SAND:    cr = 0.274f; cg = 0.245f; cb = 0.183f; break;

if (m_Tex)    { glDeleteTextures(1, &m_Tex);     m_Tex    = 0; }
if (m_Bitmap) { DeleteObject((HBITMAP)m_Bitmap); m_Bitmap = 0; }
```

### 6-2. 그 외

- 들여쓰기는 탭. 이어지는 인자는 여는 괄호에 맞춰 정렬한다.
- 중괄호는 항상 새 줄(Allman). `if` 본문이 한 줄이어도 생략하지 않는 쪽을 기본으로 한다.
- 멤버 변수는 `m_`, 상수는 대문자 스네이크, 함수·타입은 파스칼 케이스.
- 주석은 "무엇을"이 아니라 **"왜"**를 적는다. 코드를 읽으면 아는 건 쓰지 않는다.
- 섹션 구분선(`// ────`)은 파일 안에서 흐름이 크게 바뀔 때만 쓴다.

---

## 6. 진행 상황 (작업하며 갱신)

- [x] 베이스 프로젝트 확인 및 Git 연결/푸시
- [x] 장르/시점/구현방식 결정 (오픈월드 RPG, 쿼터뷰 2.5D, 스프라이트+아이소메트릭 수식)
- [x] 세계관/톤/스토리 축 결정 (중세 판타지, 다크+유머, 크툴루식 미지의 존재)
- [x] 던전밥식 몬스터 공략 구조 결정
- [x] 싱글플레이 확정
- [x] 메인 퀘스트 트리 초안 작성
- [x] 초기 기획서 작성
- [x] 게임 루프(Update/Render, 델타타임) 구현
- [x] 쿼터뷰 투영/깊이 정렬 구현
- [x] 튜토리얼 레벨 프로토타입 — 마을/숲/호수, 5분 분량 퀘스트
- [x] 한글 텍스트 렌더링 (GDI 글리프 아틀라스)
- [x] 사후처리 파이프라인 (블룸·색보정·비네트·그레인)
- [x] 맵 4배 확장(112×112) + 충돌 공간 격자
- [x] NPC 14명, 야생 짐승(늑대·사슴·까마귀) 배회/도주 AI
- [x] 캐릭터 파츠 분리 + 절차적 걷기 애니메이션, 건물 머터리얼
- [ ] 몬스터 베스티어리 데이터 구조 + 전투
- [ ] 플레이어 시스템(스탯/인벤토리)
- [ ] 오픈월드 확장(지역 이동, 스트리밍)
- [ ] 사운드
- [ ] 퀘스트 트리 세부화(던전/지역 수, 분기 조건)
